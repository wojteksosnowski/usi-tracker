def slugify(text: str) -> str:
    """
    WARNING: THIS FUNCTION IS FORBIDDEN.
    Use IDs or portal-provided slugs instead.
    """
    raise RuntimeError(
        "KATEGORYCZNY ZAKAZ UŻYWANIA SLUGIFY! "
        "System musi polegać na ID lub slugach z portali. "
        f"Próbowano przetworzyć: '{text}'"
    )
